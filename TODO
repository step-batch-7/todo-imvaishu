--files--
--DONE--index.html
--DONE--style.css


--DONE--1)static page of TODO APP
--DONE--1.1)copy all required files from FC(server.js,app.js.....)
--DONE--1.2)Add server ,app class and handlers 
--DONE--1.3)install package.json and add .eslintrc.json.
2)should show add note option on screen
3)make Template for showing note
4)should add note to json with title
5)should show note with title and date.


data_Structure should be:

[
{
  title:__title__,
  date:__date__,
  tasks: [{task:__task__,hasDone:false},{task:__task__,hasDone:false},...],
}
];

--DONE--tools:
--DONE--checkbox
--DONE--add
--DONE--remove
--DONE--config path

--DONE-- extract classes
tests--->
1)config path for testing
2)get request
3)post request

want to change some functions in script.js to make mor generic

--DONE--css
same font for all body
reuse of classes in css
i will give all attributes in body or container and overwrite at individual level
get rid of the color and  border-bottom
change color of border

--DONE--change the content type in json

use required instead of if else in todo title

--DONE--on window load =>load the page 


------------------------------------------PAIRING STARTED------------------------------


--DONE-- writing test
--DONE--need to remove date in the data_Structure
--DONE--need to remove new dates from id 

--DONE--get rid of creating taskId and subtaskId again and again by making generic functions


remaining requirements:
--DONE--1)search by title
--DONE--2)search by task
3)login and sign up page;

----------------------------------
handle =>
--DONE--client error:if status code is other than 200 or 201
--DONE--server error:if the content of post id n ot valid
--DONE--unit tests for the classes.
--DONE--before deleting asking client/user confirm message
----------------------------
